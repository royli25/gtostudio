{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the desktop solver window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
